apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: monitoring-allow
  namespace: ems
spec:
  podSelector:
    matchLabels:
      app: prometheus
  ingress:
    - ports:
        - port: 9090
