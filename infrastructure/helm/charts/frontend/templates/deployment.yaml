apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ems-frontend.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ems-frontend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ems-frontend.name" . }}
    spec:
      containers:
        - name: frontend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 3000
