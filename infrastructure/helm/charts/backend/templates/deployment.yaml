apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ems-backend.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ems-backend.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "ems-backend.name" . }}
    spec:
      containers:
        - name: backend
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8000
