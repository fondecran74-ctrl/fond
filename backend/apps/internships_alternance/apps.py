"""App configuration for Internships & Alternance."""
from django.apps import AppConfig

class InternshipsAlternanceConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.internships_alternance"
    verbose_name = "Internships & Alternance"
