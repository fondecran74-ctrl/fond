"""App configuration for Notifications & Communication."""
from django.apps import AppConfig

class NotificationsCommunicationConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.notifications_communication"
    verbose_name = "Notifications & Communication"
