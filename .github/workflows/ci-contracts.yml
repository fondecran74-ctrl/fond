name: CI Contracts
on:
  push:
    paths: ["backend/contracts/**", "packages/contracts/**"]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Validating API contracts..."
